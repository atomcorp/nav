<!-- product-list.njk -->
{% for link in navigation %}
<ul>
	<h1>{{ link.title }}</h1>
		<ul>
    	{% for child in link['child-category'] %}
			<li><h2>{{ child.title }}</h2></li>
			<ul>
			{% for product in child['products'] %}
				<li><h3>{{ product.title }}</h3></li>
				{% else %}
				<li><h3>(Range)</h3></li>
			{% endfor %}
			</ul>
    	{% endfor %}
    	</ul>
</ul>
{% endfor %}
